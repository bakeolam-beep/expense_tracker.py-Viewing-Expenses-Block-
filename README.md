# expense_tracker.py-Viewing-Expenses-Block-
## Viewing Expenses The "View Expenses" feature reads from `expenses.csv`, skips the header, and prints all stored expenses in a readable table format.  ## Requirements - Python 3.x
